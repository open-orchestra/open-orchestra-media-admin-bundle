<% if (typeof isModal != 'undefined') { %>
    <a
        class="btn btn-default btn-in-ribbon"
        id="backModal"
    >
        {{ 'open_orchestra_backoffice.global.back_to_list'|trans }}
    </a>
<% } %>
